# -*- mode: ruby -*-
# vi: set ft=ruby :

# Vagrant API version.
VAGRANTFILE_API_VERSION = "2"

Vagrant.configure(VAGRANTFILE_API_VERSION) do |config|

  # Vagrant box: Official Ubuntu Server 14.04 LTS (Trusty Tahr).
  config.vm.box = "ubuntu/trusty64"
  config.vm.box_version = "14.04"

end
